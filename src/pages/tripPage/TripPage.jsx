import { TripPageContainer } from '../../containers/tripContainer/TripPageContainer';

export const TripPage = ()=>{
    return (
        <div>
            <TripPageContainer />
        </div>
    );
}