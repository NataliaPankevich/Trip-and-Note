import { MainPageContainer } from '../../containers/mainContainer/MainPageContainer';

export const MainPage = ()=>{
    return (
        <div>
            <MainPageContainer/>
        </div>
    );
}