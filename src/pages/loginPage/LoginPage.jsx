import { LoginPageContainer } from '../../containers/loginContainer/LoginPageContainer';

export const LoginPage = ()=>{
    return (
        <div>
            <LoginPageContainer/>
        </div>
    );
}