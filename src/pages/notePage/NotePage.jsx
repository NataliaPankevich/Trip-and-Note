import { NotePageContainer } from '../../containers/noteContainer/NotePageContainer';

export const NotePage = ()=>{
    return (
        <div>
            <NotePageContainer/>
        </div>
    );
}