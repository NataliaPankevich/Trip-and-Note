import { HomePageContainer } from '../../containers/homeContainer/HomePageContainer';

export const HomePage = ()=>{
    return (
        <div>
            <HomePageContainer/>
        </div>
    );
}